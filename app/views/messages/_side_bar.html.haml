.chat-side
  .chat-side__header
    .chat-side__header__box
      %h3.chat-side__header__box__name
        = current_user.name
      %ul.chat-side__header__box__menu
        %li.new-group
          = link_to "#", class: "ico" do
            = icon('far', 'edit')

        %li.edit-user
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: 'icon')
  .groups
    .groups__side
      .user_text 
        techcamp
      .user_grops 
        まだメッセージがありません